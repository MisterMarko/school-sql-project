CREATE TABLE dummy (
  id NUMBER GENERATED BY DEFAULT AS IDENTITY,
  shortname varchar2(1024),
  CONSTRAINT pk_dummy PRIMARY KEY (id)
);

INSERT INTO dummy(shortname) VALUES ('xyz');
COMMIT;

CREATE OR REPLACE VIEW VTimeNow AS 
SELECT SYSTIMESTAMP AS NOW, 
extract(YEAR FROM SYSTIMESTAMP) YR, 
EXTRACT(MONTH FROM SYSTIMESTAMP) MN FROM dual;

select * from VTimeNow;

SELECT * FROM DUMMY d ;